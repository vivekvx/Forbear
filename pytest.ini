[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
# The database pool is session-scoped, so fixtures and tests must share one
# event loop, or the pool's connections end up bound to a loop that is already
# closed.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
